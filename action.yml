on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron: "*/5 * * * *"
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "Testing workflow scheduler"
