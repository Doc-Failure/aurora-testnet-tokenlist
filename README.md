# aurora-testnet-tokenlist

A comprehensive list of some Aurora token addresses

always run:

- ncc build index.js

after a changes in the index.js files.
The command updates the dist folder and the workfow behaviour
